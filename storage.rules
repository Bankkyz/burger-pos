rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    function isAdmin() {
      return request.auth != null
        && request.auth.token.email != null
        && request.auth.token.email.lower() == "bankkythirddy@gmail.com";
    }

    match /{allPaths=**} {
      allow read, write: if isAdmin();
    }
  }
}
